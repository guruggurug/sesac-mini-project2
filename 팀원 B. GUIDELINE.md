# 데이터 B 조원 작업 가이드라인

## 0. 문서 목적

이 문서는 「반도체 가치투자 내비게이션」 2일 MVP에서 **데이터 B 조원**이 무엇을, 어떤 순서로, 왜 해야 하는지 정리한 실행 가이드다.

데이터 B의 핵심 역할은 다음과 같다.

> 데이터 A가 준비한 ESG·사건 데이터와 기존 주가 데이터를 받아, 가격 하방위험과 ESG 위험을 계산하고 삼성전자·SK하이닉스의 추천 보유 비중을 산출하는 모델링 담당자

데이터 B는 데이터 A의 실제 데이터가 완성될 때까지 기다리지 않는다. 먼저 샘플 데이터로 계산 엔진을 완성한 뒤, 실제 CSV가 도착하면 입력 파일만 교체한다.

---

# 1. 데이터 B의 역할 범위

## 1.1 반드시 해야 하는 일

1. 주가 CSV 구조와 품질 검증
2. 일별 수익률 계산
3. 기업별 Historical CVaR 95% 계산
4. 최대 낙폭과 하방편차 계산
5. 후보 비중별 포트폴리오 CVaR 계산
6. ESG 입력값을 읽는 인터페이스 작성
7. 포트폴리오 ESG 위험 계산
8. 현재 비중 대비 변경폭인 턴오버 계산
9. 종합 위험 목적함수 구현
10. 삼성전자 20~80% 범위 1% 단위 그리드서치
11. 추천 비중, 현재 위험, 추천 위험, 위험 감소율 계산
12. 과거 사건 후 1·3·5일 수익률, 최대 하락폭, 회복 기간 계산
13. 결과를 개발 A가 호출할 수 있는 Python 함수와 JSON 형태로 전달
14. 제약조건과 재현성 테스트

## 1.2 하지 않는 일

- ESG 보고서에서 직접 수치를 찾는 일
- 뉴스·공시 사건을 직접 선정하고 공식 확인하는 일
- OpenDART·뉴스 API 수집기 개발
- 프론트엔드 화면 구현
- 미래 주가 또는 기대수익률 예측
- 실제 매수·매도 주문 실행
- 매수·매도 수량 자동 산출
- AI가 임의로 최종 추천 비중을 결정하게 하는 일

---

# 2. 전체 협업 구조

```text
데이터 A
ESG 지표·사건·출처 검증
        ↓
데이터 B
하방위험·ESG 위험·사건 반응·최적화 계산
        ↓
개발 A
B의 함수를 API로 연결
        ↓
개발 B
추천 비중·위험·사건 흐름 화면 표시
```

데이터 B는 데이터 A에게 실제 값이 아니라 다음 세 가지를 먼저 요구한다.

1. 최종 CSV 열 이름
2. 결측값 표시 방식
3. 사건 상태 값 목록

권장 공통 값:

```text
availability: available / unavailable
status: rumor / reported / confirmed / sanctioned / resolved
data_confidence: high / medium / low
```

---

# 3. 데이터 B가 받아야 하는 입력 파일

## 3.1 주가 데이터

파일 예:

```text
data/processed/stock_prices.csv
```

권장 형식:

```csv
date,005930,000660
2023-07-03,73000,115000
2023-07-04,73000,114500
```

- `005930`: 삼성전자
- `000660`: SK하이닉스
- 날짜는 오름차순
- 가격은 숫자형
- 두 종목의 공통 거래일만 사용

현재 확보된 데이터가 2023년 7월부터라면, 사건 반응 분석도 가급적 그 이후 사건을 사용한다.

## 3.2 ESG 지표 데이터

실제 파일이 도착하기 전에는 샘플 파일을 사용한다.

```text
data/sample/esg_scores_sample.csv
```

샘플 형식:

```csv
company_id,esg_risk_score,data_confidence,availability,data_status
005930,0.40,high,available,sample
000660,0.55,medium,available,sample
```

실제 ESG 원자료가 세부 지표 단위라면, 기업별 최종 ESG 위험점수를 계산하는 별도 함수가 필요하다.

## 3.3 사건 데이터

```text
data/sample/events_sample.csv
```

샘플 형식:

```csv
event_id,company_id,event_category,event_date,status,severity,data_status
SAMPLE001,005930,occupational_safety,2024-03-15,confirmed,4,sample
```

실제 사건은 `confirmed` 또는 `sanctioned`만 점수 계산에 반영한다. `reported` 사건은 화면 경고용이며 모델 점수에는 넣지 않는다.

---

# 4. 시작 전에 고정할 규칙

## 4.1 종목 비중 제약

```text
삼성전자 비중 + SK하이닉스 비중 = 100%
각 종목 비중은 20% 이상 80% 이하
공매도 없음
```

## 4.2 비중 탐색 단위

```text
삼성전자 20%, SK하이닉스 80%
삼성전자 21%, SK하이닉스 79%
...
삼성전자 80%, SK하이닉스 20%
```

총 61개 후보를 비교한다.

## 4.3 핵심 하방위험

- 핵심: Historical CVaR 95%
- 보조: 최대 낙폭, 하방편차

## 4.4 목적함수

\[
TotalRisk(w)
=
\alpha \times NormalizedCVaR(w)
+
\beta \times ESGRisk(w)
+
\gamma \times Turnover(w)
\]

의미:

- `NormalizedCVaR`: 후보 포트폴리오의 가격 하방위험
- `ESGRisk`: 후보 비중에 따른 ESG 관리위험
- `Turnover`: 현재 비중과 추천 비중의 차이

## 4.5 중요한 해석

이 모델은 어떤 주식이 오를지 예측하지 않는다.

> 과거 가격 하방위험과 확인 가능한 ESG 관리위험을 기준으로, 삼성전자와 SK하이닉스 두 종목 안에서 상대적으로 덜 취약한 비중을 찾는다.

---

# 5. 단계별 작업

## 1단계. 주가 CSV 검증

### 할 일

- `date` 열 존재 확인
- `005930`, `000660` 열 존재 확인
- 날짜 오름차순 정렬
- 중복 날짜 검사
- 결측 가격 검사
- 숫자형 변환 가능 여부 확인
- 두 종목의 공통 거래일만 남기기

### 왜 하는가

주가 파일이 잘못되면 수익률, CVaR, 사건 반응, 최적화 결과가 모두 틀어진다. 모델링 전에 입력 데이터가 계산 가능한 상태인지 확인해야 한다.

### 산출물

```text
data/processed/validated_prices.csv
reports/price_quality_report.md
```

### 완료 기준

- 필수 열이 모두 존재한다.
- 날짜 중복이 없다.
- 가격 결측치 처리 규칙이 적용됐다.
- 두 종목의 행 수와 기간을 설명할 수 있다.

---

## 2단계. 일별 수익률 계산

### 계산식

\[
r_t = \frac{P_t}{P_{t-1}} - 1
\]

### 예시

```text
전일 종가 50,000원
당일 종가 49,000원
수익률 = -2%
```

### 왜 하는가

CVaR와 포트폴리오 위험은 주가 수준이 아니라 날짜별 가격 변화율을 사용한다.

### 산출물

```text
data/processed/daily_returns.csv
```

### 완료 기준

- 첫 거래일을 제외한 모든 날짜에 두 종목의 수익률이 있다.
- 결측치가 없다.
- 비정상적으로 큰 수익률 값이 있는지 점검했다.

---

## 3단계. 기업별 하방위험 계산

### 계산할 지표

1. Historical CVaR 95%
2. 최대 낙폭
3. 하방편차

### Historical CVaR 95% 의미

일별 수익률 중 가장 나쁜 5% 구간에 속하는 날들의 평균 손실이다.

### 최대 낙폭 의미

분석기간 중 고점에서 저점까지 가장 크게 하락한 비율이다.

### 하방편차 의미

수익률이 목표수익률 0% 아래로 내려간 정도를 측정한다.

### 왜 하는가

포트폴리오 최적화 목적함수의 핵심 축은 가격 하방위험이다. 최대 낙폭과 하방편차는 화면과 발표에서 CVaR를 이해시키는 보조지표다.

### 산출물 예시

```json
{
  "005930": {
    "cvar_95": 0.034,
    "max_drawdown": 0.28,
    "downside_deviation": 0.014
  },
  "000660": {
    "cvar_95": 0.046,
    "max_drawdown": 0.36,
    "downside_deviation": 0.019
  }
}
```

### 완료 기준

- 위험값은 손실 크기를 나타내는 양수로 반환한다.
- 빈 데이터 입력 시 명확한 오류를 낸다.
- CVaR 신뢰수준을 함수 인자로 변경할 수 있다.

---

## 4단계. 포트폴리오 수익률과 CVaR 계산

### 계산식

\[
R_{portfolio,t}
=
w_{Samsung}R_{Samsung,t}
+
w_{SK}R_{SK,t}
\]

포트폴리오 수익률 시계열을 만든 뒤 그 시계열의 CVaR를 계산한다.

### 잘못된 방식

```text
삼성전자 CVaR × 비중 + SK하이닉스 CVaR × 비중
```

### 왜 잘못됐는가

종목별 CVaR의 단순 가중평균은 두 종목이 같은 날 같이 움직이는 관계를 제대로 반영하지 못한다.

### 완료 기준

- 임의 비중 50:50, 70:30 등에 대해 포트폴리오 CVaR가 계산된다.
- 비중 합이 1이 아니면 오류를 반환한다.

---

## 5단계. ESG 샘플 입력 인터페이스 작성

### 할 일

실제 ESG 값이 없어도 CSV 또는 JSON에서 기업별 ESG 위험점수를 읽게 만든다.

### 잘못된 방식

```python
samsung_esg = 0.40
skhynix_esg = 0.55
```

### 권장 방식

```python
esg_df = pd.read_csv(esg_file_path)
```

### 왜 하는가

나중에 데이터 A의 실제 파일이 도착했을 때 모델 코드를 고치지 않고 입력 파일만 교체해야 한다.

### 완료 기준

- 샘플과 실제 파일 경로를 인자로 받을 수 있다.
- `data_status=sample`을 결과에 표시한다.
- `unavailable` 값을 처리하는 규칙이 있다.

---

## 6단계. 포트폴리오 ESG 위험 계산

### 기본 계산식

\[
PortfolioESG
=
w_{Samsung} ESG_{Samsung}
+
w_{SK} ESG_{SK}
\]

### 왜 하는가

후보 비중이 바뀌면 포트폴리오가 부담하는 ESG 위험도 함께 바뀐다.

### 중요 주의사항

- ESG 값의 방향을 통일한다: 0은 낮은 위험, 1은 높은 위험
- 두 기업만으로 Min-Max 정규화하지 않는다.
- 결측값을 상대기업 평균이나 0으로 자동 대체하지 않는다.
- 데이터 부족은 별도 불확실성 또는 신뢰도로 표시한다.

### 완료 기준

- 입력 ESG 점수가 0~1 범위인지 검사한다.
- 한 기업 데이터가 `unavailable`일 때의 동작을 정의한다.

---

## 7단계. 턴오버 계산

### 기본 계산

```text
turnover = |추천 삼성전자 비중 - 현재 삼성전자 비중|
```

두 종목 비중 합이 1인 구조에서는 삼성전자 비중 차이만 사용해도 상대 비교가 가능하다.

### 왜 하는가

위험이 조금 줄어든다는 이유만으로 70:30 포트폴리오를 갑자기 20:80으로 바꾸는 극단적 추천을 막기 위해서다.

### 완료 기준

- 현재 비중과 후보 비중이 같으면 0이다.
- 비중 차이가 커질수록 값이 커진다.

---

## 8단계. 하방위험 정규화

CVaR는 보통 0.03 수준이고 ESG 점수는 0.4 수준일 수 있어 직접 더하면 ESG가 계산을 지배할 수 있다.

### 권장 MVP 방법

사전에 정한 기준 상한으로 나누고 0~1로 제한한다.

```text
normalized_cvar = min(max(raw_cvar / reference_max, 0), 1)
```

예:

```text
reference_max = 0.10
raw_cvar = 0.035
normalized_cvar = 0.35
```

### 왜 하는가

서로 단위가 다른 CVaR와 ESG 위험을 하나의 목적함수에서 비교 가능하게 만들기 위해서다.

### 주의

`reference_max`는 결과를 보고 임의로 바꾸지 말고 팀 정책값으로 고정하고 문서화한다.

---

## 9단계. 목적함수 구현

### 함수 입력 예시

```text
returns
candidate_samsung_weight
current_samsung_weight
samsung_esg
skhynix_esg
alpha
beta
gamma
```

### 함수 출력 예시

```json
{
  "total_risk": 0.472,
  "raw_cvar": 0.036,
  "normalized_cvar": 0.36,
  "esg_risk": 0.47,
  "turnover": 0.12
}
```

### 왜 하는가

후보 비중 61개를 같은 기준으로 비교해 가장 낮은 종합위험 비중을 찾기 위해서다.

---

## 10단계. 위험 우선순위별 정책값 적용

팀이 확정한 PRD 정책값을 우선한다. 턴오버 10%를 별도로 사용할 경우 예시는 다음과 같다.

| 유형 | CVaR | ESG | 턴오버 |
|---|---:|---:|---:|
| 손실 최소화 | 0.72 | 0.18 | 0.10 |
| 균형 | 0.63 | 0.27 | 0.10 |
| ESG 중시 | 0.45 | 0.45 | 0.10 |

이 값은 학술적 정답이 아니라 MVP 정책값이다. 변경할 경우 코드, 데이터분석 정의서, 발표자료를 함께 수정한다.

---

## 11단계. 1% 그리드서치

### 탐색 범위

```text
삼성전자 비중: 0.20 ~ 0.80
증가 단위: 0.01
SK하이닉스 비중: 1 - 삼성전자 비중
```

### 각 후보에서 계산할 값

- 포트폴리오 CVaR
- 정규화 CVaR
- 포트폴리오 ESG 위험
- 턴오버
- 종합위험

### 선택 규칙

종합위험이 가장 낮은 후보를 추천 비중으로 선택한다.

### 완료 기준

- 후보 수가 61개다.
- 추천 비중 합계가 1이다.
- 각 종목 비중이 0.20~0.80이다.
- 같은 입력은 항상 같은 결과를 낸다.

---

## 12단계. 현재 포트폴리오 비중 계산

### 계산식

\[
MarketValue_i = Quantity_i \times CurrentPrice_i
\]

\[
Weight_i = \frac{MarketValue_i}{TotalMarketValue}
\]

### 중요

평균 매수가는 현재 비중 계산에 사용하지 않는다. 평균 매수가는 평가손익을 보여줄 때 사용한다.

### 완료 기준

- 보유 수량과 최신 가격으로 현재 비중을 계산한다.
- 전체 평가금액이 0이면 오류를 반환한다.

---

## 13단계. 현재 위험과 추천 위험 비교

### 계산할 값

```text
current_total_risk
optimized_total_risk
risk_reduction_rate
```

### 위험 감소율

\[
RiskReductionRate
=
\frac{CurrentRisk - OptimizedRisk}{CurrentRisk}
\]

### 완료 기준

- 위험 감소율이 숫자로 계산된다.
- 현재 위험이 0일 때 0으로 처리하거나 명확한 예외 규칙이 있다.

---

## 14단계. 과거 사건 반응 분석

데이터 A가 사건 날짜를 제공하면 B가 주가와 연결한다.

### 계산할 값

- 사건 후 1거래일 수익률
- 사건 후 3거래일 누적수익률
- 사건 후 5거래일 누적수익률
- 사건 후 최대 하락폭
- 사건 전 가격 회복까지 걸린 거래일 수
- 차트용 누적수익률 배열

### 기준 규칙

- 사건일이 휴일이면 다음 거래일을 반응 시작일로 사용
- 장 마감 시각을 알 수 없는 MVP에서는 사건 공개일 이후 첫 거래일을 반응 시작일로 통일 가능
- 주가 기간 밖 사건은 분석 대상에서 제외

### 왜 하는가

현재와 유사한 과거 사건이 발생했을 때 주가가 실제로 어떻게 움직였는지 참고 정보를 제공하기 위해서다. 이는 미래 주가 예측값이 아니다.

### 출력 예시

```json
{
  "event_id": "EVT001",
  "company_id": "000660",
  "event_date": "2024-03-15",
  "reaction_start_date": "2024-03-18",
  "return_1d": -0.018,
  "return_3d": -0.042,
  "return_5d": -0.029,
  "max_drawdown": -0.061,
  "recovery_days": 13,
  "chart_data": [
    {"day": 0, "cumulative_return": 0.0},
    {"day": 1, "cumulative_return": -0.018}
  ]
}
```

---

# 6. 실제 데이터가 도착하기 전 병렬 작업

## 데이터 A가 작업 중일 때

데이터 B는 다음을 완료한다.

```text
주가 CSV 검증
수익률 계산
기업별 CVaR·MDD·하방편차
포트폴리오 CVaR
샘플 ESG 파일 로더
ESG 위험 결합 함수
턴오버 함수
목적함수
1% 그리드서치
샘플 사건 반응 함수
테스트
```

## 데이터 A의 실제 파일 도착 후

```text
샘플 ESG 파일 → 실제 esg_indicators.csv
샘플 사건 파일 → 실제 events.csv
confirmed/sanctioned 필터 적용
결측·범위·신뢰도 처리 확인
최종 결과 재계산
```

모델을 새로 만들지 않고 입력 파일만 교체하는 구조가 목표다.

---

# 7. 개발 A에게 전달할 함수

노트북 결과가 아니라 입력과 출력이 명확한 Python 함수로 전달한다.

권장 함수:

```python
load_and_validate_prices(path)
calculate_returns(price_df)
historical_cvar(returns, confidence_level=0.95)
maximum_drawdown(returns)
downside_deviation(returns, target_return=0.0)
calculate_portfolio_returns(returns_df, samsung_weight)
calculate_portfolio_cvar(returns_df, samsung_weight)
load_esg_scores(path)
calculate_portfolio_esg_risk(samsung_weight, esg_scores)
calculate_turnover(candidate_weight, current_weight)
calculate_total_risk(...)
optimize_portfolio(...)
calculate_event_reaction(price_df, event_date, company_id)
```

## 함수 작성 원칙

- 파일 경로를 코드 내부에 고정하지 않는다.
- 입력값 형식을 함수 설명에 적는다.
- 출력은 dict, DataFrame, JSON 직렬화 가능한 값으로 반환한다.
- 오류 발생 시 원인을 알 수 있는 예외 메시지를 제공한다.
- 샘플 여부와 데이터 상태를 결과에 포함한다.

---

# 8. 최종 JSON 출력 예시

```json
{
  "current_weights": {
    "005930": 0.70,
    "000660": 0.30
  },
  "recommended_weights": {
    "005930": 0.58,
    "000660": 0.42
  },
  "weight_changes": {
    "005930": -0.12,
    "000660": 0.12
  },
  "company_risks": {
    "005930": {
      "cvar_95": 0.034,
      "max_drawdown": 0.28,
      "downside_deviation": 0.014,
      "esg_risk": 0.42
    },
    "000660": {
      "cvar_95": 0.046,
      "max_drawdown": 0.36,
      "downside_deviation": 0.019,
      "esg_risk": 0.55
    }
  },
  "current_total_risk": 0.558,
  "optimized_total_risk": 0.482,
  "risk_reduction_rate": 0.136,
  "data_status": "reviewed",
  "explanation": [
    "두 종목의 과거 가격 하방위험을 비교했습니다.",
    "검증된 ESG 관리위험을 함께 반영했습니다.",
    "현재 비중에서 과도하게 벗어나지 않도록 변경폭을 제한했습니다."
  ],
  "disclaimer": "이 결과는 삼성전자와 SK하이닉스 두 종목 안에서의 위험 기반 비중 제안이며 미래 수익률 예측이나 실제 매매 명령이 아닙니다."
}
```

---

# 9. 반드시 작성할 테스트

## 9.1 데이터 검증 테스트

- 필수 주가 열이 없으면 오류
- 날짜 중복 탐지
- 가격 문자열 변환 실패 탐지
- ESG 점수가 0~1 범위를 벗어나면 오류
- 허용되지 않은 사건 상태 탐지

## 9.2 CVaR 테스트

- 결과가 양수인지 확인
- 빈 수익률 입력 시 오류
- 신뢰수준이 0~1 범위 밖이면 오류

## 9.3 비중 테스트

- 추천 비중 합계 1
- 각 종목 비중 0.20~0.80
- 후보 수 61개

## 9.4 재현성 테스트

같은 입력을 두 번 넣었을 때 추천 비중과 위험값이 동일해야 한다.

## 9.5 사건 반응 테스트

- 휴일 사건일을 다음 거래일로 변환
- 주가 기간 밖 사건 처리
- 1·3·5일 데이터가 부족한 사건 처리

## 9.6 민감도 테스트

최소한 다음을 비교한다.

```text
CVaR 신뢰수준: 90%, 95%, 97.5%
가격 기간: 최근 1년, 전체 기간
ESG 가중치 변화
특정 사건 제거 전후
```

목표는 미래 예측 정확도가 아니라 추천이 작은 설정 변화에 과도하게 뒤집히지 않는지 확인하는 것이다.

---

# 10. 폴더 구조 권장안

초기에는 하나의 파일로 구현해도 되지만 정상 작동 후 분리한다.

```text
modeling/
├── data/
│   ├── sample/
│   ├── raw/
│   └── processed/
├── risk/
│   ├── returns.py
│   ├── cvar.py
│   ├── drawdown.py
│   ├── downside.py
│   └── esg.py
├── optimization/
│   ├── objective.py
│   └── grid_search.py
├── events/
│   └── event_study.py
├── outputs/
│   ├── risk_results.json
│   └── event_reactions.json
├── tests/
│   ├── test_prices.py
│   ├── test_cvar.py
│   ├── test_optimizer.py
│   └── test_events.py
└── README.md
```

처음에는 아래 하나로 시작해도 된다.

```text
prototype_model.py
```

---

# 11. 하루 실행 일정

## 09:00~10:00 — 입력 구조 확정

- 주가 CSV 열 확인
- 데이터 A와 ESG·사건 스키마 합의
- 샘플 ESG·사건 CSV 생성
- 결측값과 상태값 규칙 고정

### 완료 기준

샘플 파일을 코드로 읽을 수 있다.

## 10:00~12:00 — 가격 하방위험

- 주가 검증
- 일별 수익률
- CVaR 95%
- 최대 낙폭
- 하방편차

### 완료 기준

두 기업의 위험지표가 출력된다.

## 13:00~15:00 — 포트폴리오 위험

- 포트폴리오 수익률
- 후보별 CVaR
- ESG 위험 결합
- 턴오버
- 목적함수

### 완료 기준

임의 비중의 종합위험이 계산된다.

## 15:00~16:30 — 최적화

- 20~80% 1% 그리드서치
- 추천 비중
- 현재 위험과 추천 위험
- 위험 감소율

### 완료 기준

추천 비중 합계 100%, 각 비중 20~80%를 만족한다.

## 16:30~17:30 — 사건 반응 분석

- 샘플 사건 날짜 연결
- 1·3·5일 수익률
- 최대 하락폭
- 회복 기간
- 차트용 데이터

### 완료 기준

샘플 사건 한 건의 결과가 JSON으로 나온다.

## 실제 데이터 도착 후

- 샘플 파일을 실제 파일로 교체
- `confirmed`·`sanctioned` 필터 확인
- 결측·신뢰도·범위 불일치 처리
- 최종 JSON 생성
- 개발 A에게 함수 전달

---

# 12. 조장에게 중간 보고할 내용

다음 형식으로 간단히 보고한다.

```text
1. 주가 데이터 검증 완료 여부
2. 계산 완료된 위험지표
3. 최적화 함수 동작 여부
4. 현재 샘플 데이터 사용 여부
5. 실제 A 데이터 연결 시 필요한 추가 필드
6. 막힌 오류와 필요한 협업
```

예:

```text
- 주가 CSV 검증 완료
- 수익률, CVaR, MDD 계산 완료
- 샘플 ESG로 1% 그리드서치 동작
- 사건 반응 함수는 1·3·5일 수익률까지 완료
- A에게 company_id, event_date, status 필드 필요
- 현재 결과는 sample 상태
```

---

# 13. 최종 완료 체크리스트

## 입력

- [ ] 주가 CSV를 코드로 정상 로딩한다.
- [ ] ESG CSV를 파일 경로로 읽는다.
- [ ] 사건 CSV를 파일 경로로 읽는다.
- [ ] 샘플과 실제 데이터 상태를 구분한다.

## 하방위험

- [ ] 일별 수익률을 계산한다.
- [ ] CVaR 95%를 계산한다.
- [ ] 최대 낙폭을 계산한다.
- [ ] 하방편차를 계산한다.
- [ ] 후보 비중별 포트폴리오 CVaR를 계산한다.

## 최적화

- [ ] ESG 위험을 비중가중한다.
- [ ] 턴오버를 계산한다.
- [ ] 종합 목적함수를 계산한다.
- [ ] 61개 후보를 평가한다.
- [ ] 추천 비중 합계가 100%다.
- [ ] 각 종목 비중이 20~80%다.
- [ ] 현재·추천 위험과 위험 감소율을 출력한다.

## 사건 반응

- [ ] 사건 후 1일 수익률을 계산한다.
- [ ] 사건 후 3일 수익률을 계산한다.
- [ ] 사건 후 5일 수익률을 계산한다.
- [ ] 최대 하락폭을 계산한다.
- [ ] 회복 기간을 계산한다.
- [ ] 차트용 누적수익률 데이터를 생성한다.

## 품질

- [ ] 같은 입력에 같은 결과가 나온다.
- [ ] 결측값을 임의로 평균 대체하지 않는다.
- [ ] `reported` 사건을 점수에 반영하지 않는다.
- [ ] 샘플 데이터는 화면에 샘플로 표시할 수 있다.
- [ ] 개발 A가 호출할 수 있는 함수 형태로 전달한다.
- [ ] 면책 문구를 결과에 포함한다.

---

# 14. 바이브 코딩용 프롬프트

```text
나는 삼성전자와 SK하이닉스 두 종목의 위험 기반 포트폴리오 최적화 모델을 만들고 있다.

Python과 pandas, numpy만 우선 사용해서 다음 기능을 모듈형 함수로 작성해줘.

입력:
1. date, 005930, 000660 열을 가진 주가 CSV
2. company_id, esg_risk_score, data_confidence, availability 열을 가진 ESG CSV
3. event_id, company_id, event_date, status, severity 열을 가진 사건 CSV

구현 기능:
1. 주가 데이터 검증
2. 일별 수익률 계산
3. Historical CVaR 95%
4. 최대 낙폭
5. 하방편차
6. 후보 비중별 포트폴리오 수익률과 CVaR
7. 비중가중 ESG 위험
8. 현재 비중 대비 턴오버
9. Normalized CVaR + ESG Risk + Turnover 목적함수
10. 삼성전자 비중 20~80%, 1% 단위 그리드서치
11. 추천 비중, 현재 위험, 추천 위험, 위험 감소율 반환
12. 사건 후 1·3·5일 수익률, 최대 하락폭, 회복 기간 반환

필수 규칙:
- 삼성전자와 SK하이닉스 비중 합은 1
- 각 종목 비중은 0.2~0.8
- 같은 입력에 같은 결과
- reported 사건은 점수 계산에서 제외
- unavailable ESG 값은 임의 평균이나 0으로 대체하지 않음
- 모든 함수에 한국어 주석과 타입 힌트 작성
- 파일 경로를 코드에 하드코딩하지 않음
- 오류가 발생하면 원인을 설명하는 ValueError 발생
- 결과는 JSON 직렬화 가능한 dict로 반환
- 샘플 데이터 여부를 data_status에 포함
- pytest 테스트 코드도 함께 작성
```

---

# 15. 핵심 원칙 요약

```text
A가 실제 데이터를 완성할 때까지 기다리지 않는다.
샘플 데이터로 계산 엔진을 먼저 완성한다.
주가 자체가 아니라 수익률로 위험을 계산한다.
포트폴리오 CVaR는 날짜별 포트폴리오 수익률에서 계산한다.
ESG와 CVaR의 단위를 맞춘 뒤 결합한다.
현재 비중과 너무 멀어지지 않도록 턴오버를 반영한다.
추천은 20~80% 범위의 1% 그리드서치로 구한다.
과거 사건 반응은 참고 정보이지 미래 예측이 아니다.
노트북 결과가 아니라 재사용 가능한 함수로 개발 A에게 전달한다.
```

---

# 16. 참고 문서

- `deep-research-report_korean-semiconductor-revised.md`
- `데이터분석정의서_반도체가치투자내비게이션_2일4인핵심기능.docx`
- `semiconductor_navigation_2day_4person_execution_plan_v2.md`

